name: "Conditional envs"

on:
  push:

jobs:
  test:
    strategy:
      matrix:
        brand: [ argos, habitat ]

    uses: ./.github/workflows/re-usable-matrix-output-workflow.yml

  pick:
    name: Docker image names
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        brand: [ argos, habitat ]

    steps:
      - run: echo ${{ needs.job1.outputs.argosTag }} ${{ needs.job1.outputs.habitatTag }}

