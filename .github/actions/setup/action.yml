name: setup
author: Sean Darley
description: setup workflow properties
inputs:
  deploy-to:
    description: envivronment to deploy to
    required: false
    default: ''
  branch:
    description: current branch
    required: false
    default: ''
outputs:
  webpack-build-env:
    description: webpack build environment
  clean-deploy-suffix:
    description: all lowercase no forward slash
runs:
  using: node16
  main: dist/index.js
